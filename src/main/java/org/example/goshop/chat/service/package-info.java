/**
 * 聊天模块的核心业务与事务层。
 *
 * <p>该包负责会话成员鉴权、消息幂等、三类消息校验、订单归属校验、
 * OSS 图片生命周期以及已读游标单调更新。消息必须先在数据库事务中成功提交，
 * 再调用 {@code websocket} 层发布实时事件，推送失败不能回滚已经保存的消息。</p>
 */
package org.example.goshop.chat.service;
